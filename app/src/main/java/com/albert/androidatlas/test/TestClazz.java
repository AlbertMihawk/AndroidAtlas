package com.albert.androidatlas.test;

/**
 * FileName : TestClazz.java
 *
 * @author : Mihawk
 * @since : 2019-06-07
 */

public class TestClazz {
//    public static void main(String[] args) {
//
//        System.out.println((float) (Math.random() * 20 * Math.pow(-1, Math.ceil(Math.random() * 1000))));
//
//        System.out.println(rangInt(-15,30));
//        System.out.println(-15+Math.ceil((Math.abs(-15-30)+1)*Math.random()));
//    }
//
//
//    private static int rangInt(int i, int j) {
//        int max = Math.max(i, j);
//        int min = Math.min(i, j) - 1;
//        //在0到(max - min)范围内变化，取大于x的最小整数 再随机
//        return (int) (min + Math.ceil(Math.random() * (max - min)));
//    }
}
